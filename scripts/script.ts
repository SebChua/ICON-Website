$(document).ready(() => {
    DOMBuilder.buildDOM();
});

class DOMBuilder {
    static buildDOM() {
        let contact_section = $('body').find('#contact-section');
        // contact_section.html("HELLO");
    }
}